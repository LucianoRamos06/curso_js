/**
 * as variaveis no js são de tipagem fraca, o que significaca que o tipo da linguagem é definida
 * em tempo de execução
 */

let qualquer = "texto qualquer"

console.log(qualquer)
console.log(typeof qualquer)

qualquer = 3.3

console.log(qualquer)
console.log(typeof qualquer)